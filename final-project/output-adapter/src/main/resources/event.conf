version = 1.0.0
transform-config {
  type-1 = {
    target-topic = flink-ml-result
    transform-function = as-is
    form = string
  }
  default = {
    target-topic = output-adapter-error
    transform-function = as-is
    form = string
  }
}
